{
  "name": "horror",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "rm -r out/ && next build && next export && fsutil file createnew out/.nojekyll 0 && git add out/ && git commit -m 'deploy gh-pages' && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "12.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "8.10.0",
    "eslint-config-next": "12.1.0",
    "gh-pages": "^3.2.3"
  },
  "homepage": "https://winters0727.github.io/horror-developer"
}
